apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-configuration-service
  namespace: annual-sports
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-configuration-service
  template:
    metadata:
      labels:
        app: event-configuration-service
    spec:
      containers:
        - name: event-configuration-service
          image: your-registry/annual-sports-event-configuration-service:latest
          ports:
            - containerPort: 8005
          envFrom:
            - configMapRef:
                name: annual-sports-config
            - secretRef:
                name: annual-sports-secrets
          env:
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: annual-sports-secrets
                  key: MONGODB_URI
            - name: DATABASE_NAME
              valueFrom:
                configMapKeyRef:
                  name: annual-sports-config
                  key: EVENT_CONFIGURATION_DATABASE_NAME
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: annual-sports-config
                  key: EVENT_CONFIGURATION_REDIS_URL
---
apiVersion: v1
kind: Service
metadata:
  name: event-configuration-service
  namespace: annual-sports
spec:
  selector:
    app: event-configuration-service
  ports:
    - name: http
      port: 8005
      targetPort: 8005
