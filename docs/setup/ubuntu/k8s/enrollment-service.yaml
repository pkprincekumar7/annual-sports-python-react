apiVersion: apps/v1
kind: Deployment
metadata:
  name: enrollment-service
  namespace: annual-sports
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enrollment-service
  template:
    metadata:
      labels:
        app: enrollment-service
    spec:
      containers:
        - name: enrollment-service
          image: your-registry/annual-sports-enrollment-service:latest
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: annual-sports-config
            - secretRef:
                name: annual-sports-secrets
          env:
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: annual-sports-secrets
                  key: MONGODB_URI
            - name: DATABASE_NAME
              valueFrom:
                configMapKeyRef:
                  name: annual-sports-config
                  key: ENROLLMENT_DATABASE_NAME
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: annual-sports-config
                  key: ENROLLMENT_REDIS_URL
---
apiVersion: v1
kind: Service
metadata:
  name: enrollment-service
  namespace: annual-sports
spec:
  selector:
    app: enrollment-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002
